<%= embed_javascript 'ajax_user_form' %>

<%= semantic_form_for @user, method: :post, html: {class: "form-horizontal"} do |f| %>
  <%= render :partial => 'form',
    :locals => {
      f: f,
      roles: @roles,
      schools: @schools,
      organizations: @organizations
    } %>
<% end %>
