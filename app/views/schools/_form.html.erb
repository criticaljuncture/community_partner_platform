<%= f.inputs do %>
  <%= f.input :name %>
  <%= f.input :region_id, as: :select, collection: Region.all.sort_by(&:name) %>
  <%= f.input :active %>
<% end %>

<%= f.actions do %>
  <%= f.button 'Save', class: "btn btn-success" %>
<% end %>
